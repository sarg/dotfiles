#!/bin/sh
if [ -n "$WAYLAND_DISPLAY" ]; then
    PLATFORM=wayland
else
    PLATFORM=xcb
fi
# fix for broken fonts: https://lists.gnu.org/archive/html/guix-devel/2023-01/msg00021.html
~/.guix-home/profile/bin/qutebrowser -s qt.force_platform $PLATFORM --qt-arg no-sandbox true $@
